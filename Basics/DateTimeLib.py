from datetime import datetime
from datetime import timedelta
import time

print(timedelta(days=1)/timedelta(seconds=1))
print(int(time.thread_time()))
print(timedelta(hours=1)/timedelta(milliseconds=1))